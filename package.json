{
  "name": "caloriemate",
  "version": "1.2.0",
  "private": true,
  "description": "Aahar \u00e2\u20ac\u201d Indian calorie tracker PWA with voice logging (English/Hindi), streaks, calendar and sustainable weight plans.",
  "type": "module",
  "scripts": {
    "build:data": "node data-build/build.mjs",
    "build:icons": "python scripts/brand-icons.py",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "serve": "python -m http.server 8765 --directory app",
    "android:prepare": "npm run stamp && node scripts/prepare-android.mjs",
    "android:sync": "npm run android:prepare && npx cap sync android",
    "android:apk": "npm run android:sync && cd android && gradlew assembleDebug",
    "stamp": "node scripts/stamp-sw.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
