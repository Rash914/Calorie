{
  "name": "aahar",
  "version": "1.0.0",
  "private": true,
  "description": "Aahar — Indian calorie tracker PWA with voice logging (English/Hindi), streaks, calendar and sustainable weight plans.",
  "type": "module",
  "scripts": {
    "build:data": "node data-build/build.mjs",
    "build:icons": "node scripts/make-icons.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "serve": "python -m http.server 8765 --directory app",
    "android:prepare": "node scripts/prepare-android.mjs",
    "android:sync": "npm run android:prepare && npx cap sync android",
    "android:apk": "npm run android:sync && cd android && gradlew assembleDebug"
  },
  "license": "MIT",
  "devDependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
